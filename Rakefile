#require 'rake'
#require 'spec/spectask'
#
## Runs all unit tests
#desc "Run all unit tests"
#Spec::Rake::SpecTask.new('all_unit_tests') do |t|
#   t.spec_files = FileList['spec/**/*.rb'].exclude("features")
#end